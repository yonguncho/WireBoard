# WireBoard v7.13.2

v7.13.1 보안 검수에서 남겨두었던 잔여 항목을 마무리하고, 검수 과정에서 발견된
내보내기 오류를 수정했습니다.

## 1. 라이브 캡처 status/stop 인증 추가

v7.13.1까지 `/api/capture/{id}/status`와 `/stop`은 토큰을 요구하지 않았습니다.
인증 헬퍼가 파일 상단에 불러와져 있었지만 실제로는 한 번도 호출되지 않는
상태였습니다.

- 캡처 시작 시 128비트 토큰을 발급해 응답에 포함합니다.
- `status`와 `stop`은 기존 업로드 API와 동일한 규약으로 `X-Upload-Token`
  헤더에 이 토큰을 요구합니다.
- 존재하지 않는 캡처 ID는 토큰 검사보다 먼저 404로 처리해, 토큰 유무로
  캡처 존재 여부를 알아낼 수 없게 했습니다.

v7.13.1에서 이미 교차 출처 호출 자체를 차단했기 때문에 실질적 위험은 낮았지만,
방어를 한 겹에만 의존하지 않도록 마무리했습니다.

## 2. 내보내기 오류 수정 (Excel / PDF 포맷)

`POST /api/export`에서 `format`을 `excel` 또는 `pdf`로 요청하면 **HTTP 500**이
발생했습니다. 해당 기능이 사용하는 `openpyxl`·`reportlab` 패키지가 배포 EXE에
포함되어 있지 않아, 내부 오류가 그대로 노출되던 문제입니다.

- 두 패키지를 선택적 의존성으로 명확히 규정했습니다.
- 없는 경우 **501**과 함께 어떤 패키지가 없는지 알려주는 메시지를 반환합니다.
- 소스에서 직접 실행하며 두 패키지를 설치한 환경에서는 종전과 동일하게 동작합니다.

**영향 범위 안내** — 화면에서 사용하는 기능은 영향이 없습니다.

| 기능 | 상태 |
|---|---|
| PDF 리포트 내보내기 (화면 버튼) | 정상 — 별도 구현이며 외부 패키지를 쓰지 않습니다 |
| CSV / JSON 내보내기 | 정상 |
| IOC 목록 내보내기 | 정상 |
| `POST /api/export` 의 `excel`·`pdf` 포맷 (API 전용) | 501 안내 반환 |

화면에서는 이 API를 호출하지 않으므로, 일반 사용에는 변화가 없습니다.

## 검증

- `pytest` **975 passed**, 5 skipped, 2 xfailed — 회귀 0건 (신규 13건 추가)
- 패키징된 EXE 스모크 테스트:
  DNS 리바인딩 차단(400), 교차 출처 캡처 시작 차단(403),
  업로드→분석(122 세션) 정상, PDF 리포트 정상, Excel/PDF 포맷 501 안내 정상

## v7.13.1을 아직 확인하지 않으셨다면

v7.13.1은 보안 수정 릴리스였습니다. **v7.13.0 이하에서는 외부 위협 인텔리전스
조회가 기본으로 켜져 있어, 분석 시 캡처에서 선택된 IP 주소 한 개가 외부
서비스로 전송되었습니다.** 캡처 파일 자체는 전송되지 않았지만, 해당 IP가
내부망 주소인 경우가 많았습니다. v7.13.1부터 기본 비활성으로 변경되었습니다.
자세한 내용은 v7.13.1 릴리스 노트를 참고해 주세요.
