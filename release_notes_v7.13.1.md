# WireBoard v7.13.1 — 보안 수정 릴리스

v7.13.0 코드 전체를 대상으로 한 보안 검수에서 확인된 4건을 수정했습니다.
**v7.13.0 이하 사용자는 업데이트를 권장합니다.**

---

## ⚠️ 먼저 알려드릴 것 — "완전 오프라인" 동작이 기본값에서 지켜지지 않았습니다

v7.13.0까지 **외부 위협 인텔리전스 조회가 기본으로 켜져 있었습니다.**
분석을 실행하면 캡처에서 자동 선택된 IP 주소 한 개가 외부 서비스로 전송되었습니다.

| 전송 대상 | 내용 |
|---|---|
| ip-api.com | 조회 대상 IP (**평문 HTTP**) |
| URLhaus (abuse.ch) | 조회 대상 IP |
| AbuseIPDB | 조회 대상 IP (`ABUSEIPDB_API_KEY` 설정 시) |
| Feodo Tracker | 블록리스트 다운로드 (IP 전송 없음) |

**정확히 무엇이 나갔고 무엇은 나가지 않았는지:**

- 나간 것: **IP 주소 문자열 한 개** (사용자가 지정했거나, 캡처에서 가장 빈번한 src/dst IP)
- 나가지 않은 것: 캡처 파일, 패킷 페이로드, 세션 목록, 파일명, 호스트명 — **캡처 파일 자체는 어떤 경우에도 PC를 벗어나지 않았습니다**

다만 캡처에서 자동 선택된 IP는 **고객사 내부망 주소(10.x, 192.168.x 등)인 경우가 많고**,
사설 IP를 걸러내는 필터가 없었습니다. 폐쇄망 규정이나 고객사 계약을 이유로
이 도구를 도입하신 경우 실질적인 문제가 될 수 있습니다.

`README.md`와 제품 화면의 "외부 API 0건 / never sent anywhere" 문구는
이 동작과 맞지 않았습니다. 문구가 아니라 **동작을 기준에 맞추는 방향으로 수정**했습니다.

**v7.13.1부터 외부 조회는 기본 비활성입니다.** 사용하시려면
`ENABLE_EXTERNAL_REPUTATION=1` 환경변수를 명시적으로 설정해야 합니다.
설정하지 않으면(또는 오타가 나면) 비활성 상태로 동작합니다.

---

## 수정 내역

### 1. 외부 조회를 옵트인으로 반전 (MEDIUM)

- `ENABLE_EXTERNAL_REPUTATION` 기본값을 `1` → `0`으로 변경하고, 판정을
  허용목록(`1`/`true`/`yes`/`on`일 때만 활성) 방식으로 뒤집어 안전한 쪽으로
  실패하도록 했습니다.
- **AbuseIPDB 조회가 이 스위치를 무시하던 문제**를 수정했습니다. 기존에는 API 키
  유무로만 판단해, 오프라인 스위치를 꺼도 키가 설정돼 있으면 IP가 계속 나갔습니다.
- Feodo 블록리스트 다운로드도 같은 스위치 아래로 넣었습니다. 단 게이트를 캐시
  조회 아래에 두어, **이미 받아둔 블록리스트는 오프라인에서도 계속 동작합니다.**

### 2. DNS 리바인딩 차단 (MEDIUM)

`127.0.0.1` 바인딩은 네트워크 도달성만 제한할 뿐, 공격자가 자신의 도메인을
`127.0.0.1`로 재해석시키는 DNS 리바인딩은 막지 못했습니다. 이 경우 악성 웹페이지가
WireBoard와 동일 출처가 되어 응답을 읽을 수 있었습니다.

- **Host 헤더 검증** 추가 — 루프백 이름이 아닌 Host를 거부합니다.
- **Origin 검증** 추가 — 교차 출처 요청을 거부합니다.

### 3. 라이브 캡처 교차 출처 시작 차단 (HIGH)

악성 웹페이지가 사용자 모르게 라이브 패킷 캡처를 시작시킬 수 있었습니다.
"JSON 본문은 CSRF에 안전하다"는 통념이 이 조합에서는 성립하지 않았습니다 —
Content-Type 헤더가 없는 본문도 JSON으로 파싱되기 때문에, preflight 없이
전달되는 요청으로 캡처를 시작시킬 수 있었습니다.

위 2번의 Origin 검증이 이 경로를 차단합니다.

### 4. 자동 종료된 캡처의 메모리 해제 (HIGH의 일부)

시간 상한으로 자동 종료된 캡처가 최대 10만 패킷을 담은 채 프로세스 종료까지
메모리에 남아 있었습니다. 동시 캡처 슬롯은 풀려 반복 시작이 가능했기 때문에
메모리가 계속 누적됐습니다. 자동 종료 후 60초 안에 수거되지 않으면 해제합니다.

### 5. ip-api 조회를 HTTPS로 전환 (LOW)

`http://` → `https://`. 외부 조회를 켠 경우, 조회 대상 IP가 평문으로 전송되던
문제를 수정했습니다.

---

## 검수 범위와 한계

`backend/` 86개 + `frontend/src/` 36개 파일을 17개 컴포넌트로 나눠 검사했습니다.

**깨끗하게 확인된 영역** — 최우선 공격면으로 지목했던 곳들입니다:

- **악성 캡처 파일 파싱**: pcap 길이 필드 경계 검사, 플로우/패킷 수 상한,
  DNS 압축 포인터 루프, TLS 확장 길이 처리 — 모두 방어되고 있었습니다.
- **XSS**: 프런트엔드 전체에 raw HTML 삽입 지점이 없습니다. 공격자 제어도가
  가장 높은 디코딩된 패킷 페이로드도 React 기본 이스케이핑을 거칩니다.
- **출력물 인젝션**: CSV 수식 인젝션, PDF 이스케이핑, Suricata/Snort 룰 생성 —
  모두 방어되고 있었습니다.
- **경로 조작, 라이선스 서명 검증, 하드코딩된 비밀정보**: 문제 없음.

**한계**: 이 검수는 코드를 읽어 수행했으며 실제 익스플로잇을 실행해 검증하지
않았습니다. 후보 항목 일부는 검증 정족수를 채우지 못해 보고에서 제외되었으며,
이는 "안전함이 확인된 것"이 아니라 "완전히 검증되지 않은 것"입니다.

---

## 검증

- `pytest` **962 passed**, 5 skipped, 2 xfailed — 회귀 0건
  (기존 921 + 신규 41. 기존 테스트의 단언을 약화한 곳은 없습니다)
- 패키징된 EXE 스모크 테스트 통과: 리바인딩 차단(400), 교차 출처 캡처 시작
  차단(403), 정상 요청(200), 업로드→분석 E2E(122 세션) 정상

## 알려진 잔여 사항

- `/api/capture/{id}/status`와 `/stop`은 여전히 토큰을 요구하지 않습니다.
  교차 출처 경로는 이번 수정으로 차단되었고, 남은 경로는 추측 불가능한
  UUID를 알아야 하므로 위험도가 낮다고 판단해 다음 릴리스로 미뤘습니다.
- Excel 내보내기는 패키징 누락으로 배포본에서 동작하지 않을 가능성이 있습니다
  (별도 확인 예정). JSON·CSV·PDF 내보내기는 정상입니다.
